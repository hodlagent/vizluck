<script lang="ts">
  // Placeholder tab — intentionally empty for now.  The `<script>` block is
  // not decorative: without it the language tools emit no declaration for this
  // module and `import Game from "./tabs/Game.svelte"` fails to type-check.
</script>

<div class="empty">Game — coming soon</div>
